export const PromedioRanking = ({ promedio }: { promedio: string }) => {
    return (
        <div>
            <h3>Calculo de Promedio y Ranking</h3>
            <h1>Promedio: {promedio}</h1>
        </div>
    );
};
